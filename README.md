# Pyramid Casino — เดโมเว็บหน้าเดียว (Frontend เท่านั้น)

เว็บเดโม Casino แบบเล่นคนเดียว ทำงานบนฝั่งเบราว์เซอร์ 100% โดยไม่ต้องมีระบบสมาชิก ไม่ต้องเชื่อมต่อฐานข้อมูล และไม่ต้องมีเซิร์ฟเวอร์ฝั่งแบ็กเอนด์

เหมาะสำหรับการทดลอง UX/UI ของเกมคาสิโน การสาธิตฟีเจอร์ และการรันแบบสแตติกไฟล์ได้ทันที

## 🚀 ฟีเจอร์หลัก

- เดโมเกมคาสิโนหลายประเภท พร้อมหน้าเกมแยกไฟล์ใน `frontend/html/games/*`
- หน้าโหลดก่อนเข้าเกม (Loading Overlay) แสดงข้อความ “กำลังโหลดเกม...” ~1.5s
- หน้า “เติมเงิน” แบบเดโม ใช้ `localStorage.demo_balance` สำหรับยอดเงินจำลอง
- เนวิเกชัน, แอนิเมชัน, และเอฟเฟกต์ UI ครบถ้วนโดยไม่พึ่ง API
- รันแบบสแตติกไฟล์ได้ทันที เปิด `index.html` ตรง ๆ ก็ใช้งานได้

## 🛠️ เทคโนโลยีที่ใช้

- **HTML5/CSS3/JavaScript** ล้วน ๆ
- **Assets** รูปภาพ/ไอคอน พร้อมในโฟลเดอร์ `frontend/images`
- ไม่ใช้เฟรมเวิร์กฝั่งเซิร์ฟเวอร์ และไม่มีฐานข้อมูล

## � การเตรียมใช้งาน

- เว็บเบราว์เซอร์สมัยใหม่ (Chrome/Edge/Firefox)
- ถ้าต้องการรันผ่านพอร์ต `localhost`: มี Python 3 เพื่อเปิดเซิร์ฟเวอร์สแตติกแบบง่าย

## 🚀 เริ่มต้นใช้งานทันที

### วิธีที่ 1: เปิดไฟล์ตรง (ง่ายสุด)
- ดับเบิลคลิก `frontend/index.html`

### วิธีที่ 2: เปิดเซิร์ฟเวอร์สแตติกด้วย Python
- เปิด PowerShell ที่โฟลเดอร์ `frontend`
- รัน: `py -m http.server 3001`
- เปิด: `http://localhost:3001/`

### วิธีที่ 3: ส่วนขยาย Live Server (ถ้าใช้ VS Code)
- ติดตั้ง Live Server
- เปิดโฟลเดอร์ `frontend` แล้วคลิก “Go Live”

## � โครงสร้างโปรเจกต์ (Frontend เท่านั้น)

```
Casino/
├── frontend/
│   ├── html/            # หน้าเว็บทั้งหมด
│   │   ├── all-games.html
│   │   ├── deposit.html
│   │   └── games/       # หน้าเกมแต่ละประเภท
│   │       ├── blackjack.html
│   │       ├── baccarat.html
│   │       ├── slot.html
│   │       ├── crash.html
│   │       ├── dicesum.html
│   │       ├── coinflip.html
│   │       ├── keno.html
│   │       ├── mine.html
│   │       ├── poker.html
│   │       ├── ringoffortune.html
│   │       └── colorcrash.html
│   ├── css/             # สไตล์รวมและสไตล์เกม
│   ├── js/              # สคริปต์รวมและสคริปต์เกม
│   ├── images/          # รูปภาพและแอสเซ็ต
│   └── index.html       # หน้าแรกของเว็บ
└── ... (ไฟล์อื่นที่ไม่จำเป็นกับเดโมสามารถละได้)
```

## 🎮 รายละเอียดสำคัญของเดโม

- หน้าโหลดก่อนเข้าเกม: เพิ่มผ่านสคริปต์กลาง `frontend/js/script.js:302-330`
  - ตรวจจับทุกหน้าใน `frontend/html/games/*.html`
  - ซ่อน `div.game-container` ชั่วคราว, แสดงโอเวอร์เลย์, แล้วปล่อย UI กลับมาหลัง ~1.5s

- หน้าฝากเงิน (เดโม): ใช้ `localStorage.demo_balance`
  - อ่าน/อัปเดตยอดเงินที่ `frontend/html/deposit.html:347-390`
  - แสดงยอดปัจจุบันใน element `#currentBalance`

- ไม่มีระบบสมาชิก/เข้าสู่ระบบ: UI และสคริปต์ที่เกี่ยวข้องถูกลบแล้วจากทุกหน้า

- การเพิ่มเกมใหม่:
  - สร้างไฟล์ใน `frontend/html/games/` และเชื่อม `../../css/styles.css`, `../../css/game.css`
  - เชื่อม `../../js/script.js` เพื่อได้หน้าโหลดอัตโนมัติ
  - เพิ่มสคริปต์เกมของคุณเองใน `frontend/js/`

## 🧪 การทดสอบ/การตรวจสอบ

- เปิดหน้า `index.html` ตรวจดูแบนเนอร์, เมนู, และแอนิเมชัน
- เปิด `html/all-games.html` และทดลองปุ่ม “เล่นเลย” แต่ละเกม (โหลดสั้น ๆ และเข้าเกม)
- เปิดหน้าเกม เช่น `html/games/blackjack.html` ควรมีหน้าโหลดก่อนเห็น UI เกม
- เปิด `html/deposit.html` ทดลองกรอกจำนวนและวิธีชำระ ยอดเดโมจะถูกเก็บใน `localStorage.demo_balance`

#### Games
- `GET /api/v1/games` - Get all games
- `GET /api/v1/games/:id` - Get game by ID
- `GET /api/v1/games/featured` - Get featured games
- `GET /api/v1/games/popular` - Get popular games
- `GET /api/v1/games/search` - Search games
- `GET /api/v1/games/statistics` - Get game statistics

#### Transactions
- `GET /api/v1/transactions/history` - Get transaction history
- `POST /api/v1/transactions/deposit` - Create deposit
- `POST /api/v1/transactions/withdraw` - Create withdrawal
- `GET /api/v1/transactions/statistics` - Get transaction statistics
- `GET /api/v1/transactions/:id` - Get transaction by ID

#### Admin
- `GET /api/v1/admin/dashboard` - Get admin dashboard
- `GET /api/v1/admin/users` - Get all users
- `GET /api/v1/admin/users/:id` - Get user by ID
- `PUT /api/v1/admin/users/:id/toggle-status` - Toggle user status
- `GET /api/v1/admin/games` - Get all games
- `PUT /api/v1/admin/games/:id/toggle-status` - Toggle game status
- `GET /api/v1/admin/transactions` - Get all transactions
- `PUT /api/v1/admin/transactions/:id/process` - Process transaction

## 🧪 Testing

Run the test suite:

```bash
# Run all tests
npm test

# Run tests in watch mode
npm run test:watch

# Run tests with coverage
npm test -- --coverage
```

## 🔧 แนวทางพัฒนาเพิ่มเติม

- ปรับเวลาโหลดหน้าเกมโดยแก้ช่วงเวลาใน `frontend/js/script.js:326-329`
- ใช้ `localStorage.demo_balance` แทนยอดเงินจริงเพื่อคงการเป็นเดโม
- ไม่ควรอ้างอิง API หรือระบบสมาชิกในเดโมนี้

## 🖥️ การรองรับเบราว์เซอร์
- Chrome/Edge/Firefox เวอร์ชันล่าสุด
- Safari เวอร์ชันล่าสุด (อาจมีความแตกต่างด้านฟอนต์/แอนิเมชันเล็กน้อย)

## ⚠️ ข้อจำกัดของเดโม
- ไม่มีระบบผู้ใช้/ยอดเงินจริง/ธุรกรรมจริง
- ยอดเงินทุกอย่างเป็นข้อมูลจำลองในเบราว์เซอร์
- เหมาะสำหรับสาธิตและทดสอบ UX/UI เท่านั้น

## 🆘 การสนับสนุน
- เปิด Issue พร้อมแนบภาพหน้าจอ/คอนโซล หากพบปัญหาในการรันสแตติกไฟล์
- ระบุเบราว์เซอร์และขั้นตอนที่ทำ เพื่อช่วยแก้ปัญหาได้เร็วขึ้น

## 📋 License
โครงการนี้อยู่ภายใต้สัญญาอนุญาต MIT — ดูไฟล์ `LICENSE`

## 🤝 การมีส่วนร่วม
- ยินดีรับข้อเสนอปรับปรุง UI/UX และหน้าเกมเพิ่มเติมสำหรับเดโม

## 📝 License

This project is licensed under the MIT License - see the [LICENSE](LICENSE) file for details.

## 📣 หมายเหตุปิดท้าย
- เดโมนี้เน้นความเรียบง่าย ใช้งานง่าย และไม่มีการเก็บข้อมูลส่วนบุคคล
- หากต้องการยกระดับเป็นเว็บเต็มระบบ สามารถเพิ่มแบ็กเอนด์ในอนาคตได้

**ขอให้สนุกกับการทดลองเล่น! 🎰✨**


